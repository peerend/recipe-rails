<h1><%= @entry.name %></h1>
<h3>Rating (1 to 5 Stars): <%=@entry.average_rating%><br> Total Ratings: <%=@entry.total_ratings%></h3>
<h3>Tags:
<% @entry.tags.each do |tag| %>
  <span><%= tag.name + " " %></span>
<%end%>
</h3>

<h3>Recipe:</h3>
<p><%= @entry.description %></p>


<h4>Add a Rating:</h4>

<%= render('layouts/errors', :object => @rating) %>

<form action="/ratings/<%=params[:id]%>" method="post">
  <label for="rating">Rating</label>
  <input id="rating" name="rating[stars]" type="number" value="<%= @rating.stars %>">
<button class ="btn btn-primary">Add Rating</button>
</form>
<br>
<p><a href="/edit/<%= @entry.id %>"><button class ="btn btn-primary">Edit this recipe</button></a></p>
